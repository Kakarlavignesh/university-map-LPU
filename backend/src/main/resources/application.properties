spring.application.name=map-optimizer
server.port=8081

# H2 Database Configuration (Zero-config for demo)
spring.datasource.url=jdbc:h2:mem:university_map;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# MySQL Configuration (Commented out for now)
# spring.datasource.url=jdbc:mysql://localhost:3306/university_map?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=password

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
